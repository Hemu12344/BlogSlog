<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Reset Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeIn {
      0% {
        opacity: 0;
        transform: scale(0.95);
      }

      100% {
        opacity: 1;
        transform: scale(1);
      }
    }

    .fade-in {
      animation: fadeIn 0.5s ease-out forwards;
    }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-pink-100 via-purple-100 to-blue-100 p-4">

  <form action="/newpass" method="POST"
    class="fade-in bg-white p-8 rounded-3xl shadow-xl w-full max-w-md space-y-6 border border-gray-200">
    <h2 class="text-2xl font-bold text-center text-blue-700">Reset Password</h2>

    <div class="text-center text-gray-700 font-medium">
      User: <span class="text-purple-600"><%= user.username %></span>
    </div>

    <input type="text" name="userId" value="<%= user._id %>" hidden />

    <div class="space-y-4">
      <input type="password" name="password" placeholder="New Password" required
        class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-400 transition" />

      <input type="password" name="password2" placeholder="Repeat New Password" required
        class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-400 transition" />
    </div>
    <div class="flex gap-5">
        <input type="submit" value="Submit"
      class="w-full bg-blue-600 text-white font-semibold py-3 rounded-xl hover:bg-blue-700 transition-all duration-300 transform hover:scale-105 cursor-pointer shadow-md" />
    <a href="/login" class="w-full bg-red-600 text-white font-semibold text-center py-3 rounded-xl hover:bg-blue-700 transition-all duration-300 transform hover:scale-105 cursor-pointer shadow-md">Login with pass ?</a>
    </div>
  </form>

</body>

</html>
