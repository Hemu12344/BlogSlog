<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeIn {
      0% {
        opacity: 0;
        transform: scale(0.95);
      }

      100% {
        opacity: 1;
        transform: scale(1);
      }
    }

    .fade-in {
      animation: fadeIn 0.5s ease-out forwards;
    }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-indigo-100 to-purple-100 p-4">

  <form action="/forgot" method="POST"
    class="fade-in bg-white p-8 rounded-3xl shadow-xl w-full max-w-md space-y-6 border border-gray-200">
    <h2 class="text-2xl font-bold text-center text-purple-700">Forgot Password</h2>

    <input type="email" name="email" placeholder="Enter your email" required
      class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-400 transition text-gray-800" />

    <button type="submit"
      class="w-full bg-purple-600 text-white font-semibold py-3 rounded-xl hover:bg-purple-700 transition-all duration-300 transform hover:scale-105 shadow-md">
      Find Account
    </button>

    <div class="text-center text-gray-600">
      <p>Don't have an account?
        <a href="/signup" class="text-purple-600 hover:underline font-medium">Signup here</a>
      </p>
    </div>
  </form>

</body>

</html>
